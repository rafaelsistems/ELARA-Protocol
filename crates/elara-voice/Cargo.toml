[package]
name = "elara-voice"
version = "0.1.0"
edition = "2021"
description = "ELARA Voice State - Parametric voice encoding as state"
license = "MIT OR Apache-2.0"

[dependencies]
elara-core = { path = "../elara-core" }
rand = "0.8"

[dev-dependencies]
